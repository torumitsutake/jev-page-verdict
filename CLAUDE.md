# CLAUDE.md

このリポジトリで作業するときに最初に読むファイル。設計判断とその理由、および
「変えてはいけない理由があるもの」を記録してある。

## これは何か

開いているページが「宣伝」なのか「実体験の感想」なのかを判定する Chrome 拡張（MV3）。
判定には Jev（TypeSafe AI）を使う。Jev は 2026年9月15日公開の判定専用モデルで、
文章を生成せず、選択・採点・真偽の答えを確率つきで返す。

- HTTP API: `POST https://api.typesafe.ai/v1/systemone`
- 料金: 入力100万トークンあたり $0.042、出力は無課金
- 公式ドキュメント: https://docs.typesafe.ai/

## 動かす

```
# chrome://extensions → デベロッパーモード ON
# → 「パッケージ化されていない拡張機能を読み込む」でこのフォルダを選択
# → 拡張の「オプション」で TypeSafe API キーを保存
```

ビルド不要。素の JS モジュールで動く。編集したら拡張カードのリロードボタンを押す
（service worker は自動再読込されない）。

## ファイル構成

```
manifest.json    MV3 マニフェスト
logo.png         ロゴ原寸。icons/ はここから sips で書き出す
extract.js       ページから判定材料を抜き出す（executeScript で注入される関数）
questions.js     質問定義・ラベル辞書・プリセット・閾値・色。調整はここに集約
i18n.js          UI の文言だけ。判定に関わる定義は置かない
background.js    API 呼び出し、キャッシュ、バッジ、検索結果の判定、本文の取得
offscreen.html/js  取得した HTML のパース専用。service worker に DOMParser が無い
serp.js/.css     Google 検索結果の色分け。設定でオンにしたときだけ動的登録される
popup.html/js    判定結果の表示
options.html/js  APIキー・分類設定・表示言語・表示設定
```

## 変えないでほしい設計判断

以下は検討の結果そうしてある。変更する場合は理由を確認すること。

### 1. ジャンルは既定6分類。増やすなら軸を増やす

Choice は選択肢上の確率分布を返すため、**意味が重なるラベルが2つあると確率がそこで
割れ、モデルが正しく理解していても confidence が閾値を割る**。「宣伝」「アフィリエイト」
「通販」を並べると 0.4 / 0.35 / 0.15 に散って「判定できず」になる。

解像度が欲しいときはラベルではなく軸を足す。質問は並列評価されるので応答時間は
ほとんど伸びない。現在4軸（`genre` / `stance` / `publisher` / `product`）で数百通り。
「アフィリエイト記事」は `genre=commerce × stance=incentivized × publisher=individual`
として出るので、専用ラベルは要らない。

`GENRE_CATALOG` の `splits` フィールドは親子関係の宣言で、両方有効になると
設定画面が警告を出す。新しいラベルを足すときは `splits` も正しく設定すること。

### 2. instructions と criteria は英語、state はそのままの言語

公式ドキュメントに「主な学習言語は英語で、他の言語では精度が下がる」と明記がある。
質問側だけ英語にしてある。日本語に戻さないこと。**表示言語を日本語に切り替えても
質問は英語のまま**であること（`GENRE_CATALOG` の `name` が表示用、`criteria` が Jev 用）。

### 3. 数えれば分かるものは Jev に聞かない

アフィリエイトリンク本数、価格表記、CTA 文言、schema.org の型は `extract.js` で
数えて state に入れている。無関係な情報が増えるほど精度が落ちると公式が明記して
いるので、state に何か足すときは「その情報が判断に必要か」を確認すること。

### 4. 既定は手動起動（activeTab）

`content_scripts` で `<all_urls>` に常駐させれば自動判定できるが、その瞬間に
閲覧した全ページの本文が外部 API に流れる。既定は意図的にアイコンを押したページ
だけにしてある。自動化する場合は `optional_host_permissions` で明示的に許可を取る形にする。

### 5. キャッシュキーにラベル構成のハッシュと判定元を混ぜてある

`configFingerprint()` の値と `src`（`page` / `snippet`）がキーに入っている。前者が
ないと、分類設定を変えたのに古い判定が出続けて原因不明のバグになる。後者がないと、
検索結果のスニペットから出した弱い推定が、そのページを開いたときの本判定として
出続ける。どちらも外さないこと。

### 6. confidence が低いときは断定しない

`CONFIDENCE.assert` (0.75) 以上で断定、`hint` (0.5) 以上で「〜らしい」、
それ未満は「判定できず」でユーザーに判断を返す。勝手に上位1件を表示する実装に
戻さないこと。

### 7. 「自動生成か」は genre に置かない

品質の属性であって目的ではないので、genre のどのラベルとも直交する。ジャンルに置くと
**他の全ラベルと同時に重なる**（設計判断1の最悪版）。`machine_written` という独立した
Noul にしてあるので、genre 側に戻さないこと。`thin_content`（水増し・引き写し）とは
別の問いなので、まとめないこと。

criteria は「AI っぽいか」ではなく、state から確かめられる証拠で書くこと
（署名の不在、日付や当事者しか知らない具体の不在、均一な節構成、断定を避けた一般論）。

### 8. product 軸は stance と別物。統合しないこと

`product`（公式 / 正規販売店 / マーケットプレイス出品 / 第三者の宣伝 / 商品ページではない）は
「その商品の作り手との関係」を聞いている。`stance` の「書き手の利害」とは別軸で、正規販売店は
`stance=seller` だが作り手ではなく、公式ストアとマーケットプレイス出品は stance では割れない。
まとめると設計判断1のラベル重なりを自分で作ることになる。

「商品ページではない」が該当なしにあたる。これを外すと、商品と無関係なページで確率が
他の4つに散って毎回 confidence を割る。

### 9. 検索結果の色分けは3段階で、既定は全部オフ

`serp.enabled` はキャッシュにある判定だけを描く（送信ゼロ）。`serp.snippet` を足すと
未判定の結果をスニペットから推定する。`serp.fetch` を足すと、押した1件だけ本文を取得する
（設計判断10）。オンにするとき `chrome.permissions.request` で許可を取り、
`chrome.scripting.registerContentScripts` で登録する。
**マニフェストに静的な content_scripts を書かないこと**（設計判断4と同じ理由）。

要求する権限の範囲は分けてある。色分け本体は Google の検索ページだけ、本文取得だけが
全サイト（結果のドメインが事前に分からないため）。まとめて要求しないこと。

スニペット推定は材料が `extract.js` の数値を1つも含まない。断定させず点線で描き、
`CONFIDENCE.snippet` を割ったら何も描かない。間違った色を置くより無色のほうがまし。

### 10. 本文の取得はクリックした1件だけ。全件自動にしない

検索結果の本文を全件取りに行くのは、技術的には可能だが作らない。理由は4つあり、
重い順に「開いてもいないページの全文が検索のたびに外へ出る（設計判断4と同じ位置）」
「攻撃者が SEO で載せたページの本文が、人間の判断を挟まず state に入る」
「自分の IP から検索ごとに数十ドメインへ自動アクセスする挙動になる」
「Cookie もスクリプトも無い fetch では同意画面や空の SPA シェルが返り、それを
本文として判定してしまう」。コストは一番小さい問題。

取得まわりの決まり: `credentials: "omit"`（ログイン済みの中身を外に出さない）、
`fetchMaxBytes` / `fetchTimeoutMs` で打ち切り、`fetchMinBodyChars` 未満は判定せず
エラーを返す。

キャッシュの src は `fetch` で、実訪問の `page` とは**別のキーに入れたまま**にする。
そのうえで、ポップアップは `page` → `fetch` の順に見て使い回す（同じページを2回課金しない）。
使い回すときは meta に「取得した本文から」と出して、実訪問の判定として扱わないこと。
fetch で返るのはログアウト状態・スクリプト無しのページなので、同じものではない。
再判定（force）は必ず `page` を上書きする。統合して1つのキーにしないこと。

### 11. content script は色も文言も件数も持たない

`serp.js` は background が組み立てた `{ color, chip, title }` を描くだけで、
上限も `serpConfig` メッセージで受け取る。ラベル・閾値・定数を `questions.js` の外に
散らさないため。content script は ES モジュールを import できないので、この形でないと
定数が二重化する。

検索結果はそのページに出ている全件を見る（`SERP.batchSize` ずつ、`maxPerPage` が安全弁）。
レート制限とキー拒否は残り全件が同じ結果になるので、そこで打ち切ること。

## 未着手（優先順）

1. **日本語ページでの正解率測定。これが最優先。**
   50件程度の正解ラベル付き URL リストを用意し、軸ごとの正解率と confidence 分布を出す。
   閾値はその結果から決める。測る前に公開しない。
2. 階層分類の検討。公式クックブック「classification using confidence」と
   「hierarchical classification」が、確率が割れる問題への公式解。
   詳細プリセットはこれで置き換えられる可能性がある。
   https://docs.typesafe.ai/cookbooks/classification_using_confidence
3. OpenRouter / Vercel AI Gateway 対応。TypeSafe 本体は早期アクセスで
   ウェイトリスト待ちが多いため、エンドポイントと認証を設定で切り替えられるようにする。
   Gateway 経由ならリクエスト単位の Zero Data Retention も指定できる。
4. スクリーンショットか GIF。
5. eval ハーネス（URL リスト → 正解率と confidence 分布）。
6. スニペット推定と本文取得の正解率測定。実訪問の判定とは別に測る（材料が違うので別物）。
7. awesome-jev への PR。

## レビューで潰した罠（戻さないこと）

- **「該当なし」の criteria をメタな文にしない。** 「上のどれにも当てはまらない」と書くと、
  他のラベルが決定的に一致しないページで常に真になり、迷ったぶんの確率を全部吸い込む。
  実測で会社の note ページが該当なし80% / 公式情報1%未満になった。他のラベルと同じ粒度で
  「具体的にどういうページか」を書くこと（ログイン画面・エラー・検索結果一覧・同意画面など）。
- **criteria は「目的」で書かない。** 「買わせる意図で書かれた」は観測できない。state に
  入っているもの（価格・カートボタン・アフィリエイトパラメータ・日付・引用元）で書く。
  Jev は文字通りに解釈するので、書いていない基準は使われない。
- **instructions と criteria は ASCII のみ。** em dash も入れない（設計判断2の機械的な担保）。

- `cacheKey` はクエリとフラグメントを落とす。クエリで内容が変わるサイト
  （`?v=`, `?p=` など）は同じ判定を共有してしまう。README に書いてある既知の割り切り。
- `fetchState` のタイムアウトは**本文を読み終えるまで**止めない。ヘッダだけ即返して
  本文を垂れ流すサーバがあり、そこで `clearTimeout` すると応答が返らずチップが固まる。
  サイズ上限も読みながら効かせること（`res.text()` だと全部載せてから切ることになる）。
- `onMessage` の分岐には必ず `else` で応答を返す。返さないと送信側が永久に待つ。
- `t()` の置換は関数で渡す。文字列で渡すと `$&` や `$$` が展開され、独自ラベル名が壊れる。
- `pick()` は `||` で畳む。`??` だと空文字がそのまま返り、表示名がキーになる。
- `configFingerprint` は axes を並べ替えてからハッシュする。オブジェクトのキー順に
  依存させない。スニペット用は別の fp（`kind="snippet"`）。関係ない軸の切り替えで
  検索結果の推定を投げ直さないため。
- 設定画面で `config = await saveConfig(...)` と全体を置き換えない。未保存の編集が
  保存済みの値で上書きされ、画面と保存内容がずれる。
- `serp.js` の広告判定に `\b` を使わない。日本語の後ろには ASCII の語境界が無いので
  「スポンサー」「広告」に一致しない。「Ad」単体も拾わない（"Ad Age" を広告と誤認する）。
- `serp.js` はバッチが失敗したら `pvSeen` / `done` / `budget` を戻す。戻さないと
  その20件は二度と描かれない。

## 既知の弱点

- **プロンプトインジェクション。** ページ本文をそのまま state に入れている。
  表示するだけなら実害は限定的だが、判定結果で何かを自動実行する設計にするなら
  Jev の手前に対策が要る。
- **APIキーは平文。** `chrome.storage.local` に置かれるため、拡張を読み込める人は
  取り出せる。各自が自分のキーを入れる公開版では論点にならないが、組織に配布する
  場合は拡張 → 自前プロキシ → Jev の構成にしてキーをサーバ側に置くこと。
- Score は段階の「間」が小数で返る。0/1/2 の整数ではない。
- **Google の DOM 依存。** `serp.js` は `#search`/`#rso` の中の `h3` から祖先のリンクを
  辿る構造だけに頼っている（クラス名は頻繁に変わるため）。それでも Google 側の改装で
  壊れる前提で、壊れたら「色が付かない」だけで済むようにしてある。
- **スニペット推定の精度は未測定。** 本文判定より確実に落ちるが、どれだけ落ちるかは
  測っていない。

## コードの約束

- ビルドツールを入れない。素の ES モジュールのまま保つ
- 閾値・重み・ラベルは `questions.js` に集約する。他のファイルに定数を散らさない
- Jev に聞く質問は「専門家が数秒で答えられる粒度」に割る
- Choice には必ず「該当なし」を入れる
