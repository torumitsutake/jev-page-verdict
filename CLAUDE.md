# CLAUDE.md

このリポジトリで作業するときに最初に読むファイル。設計判断とその理由、および
「変えてはいけない理由があるもの」を記録してある。

## これは何か

開いているページが「宣伝」なのか「実体験の感想」なのかを判定する Chrome 拡張（MV3）。
判定には Jev（TypeSafe AI）を使う。Jev は 2026年9月15日公開の判定専用モデルで、
文章を生成せず、選択・採点・真偽の答えを確率つきで返す。

- HTTP API: `POST https://api.typesafe.ai/v1/systemone`
- 料金: 入力100万トークンあたり $0.042、出力は無課金
- 公式ドキュメント: https://docs.typesafe.ai/

## 動かす

```
# chrome://extensions → デベロッパーモード ON
# → 「パッケージ化されていない拡張機能を読み込む」でこのフォルダを選択
# → 拡張の「オプション」で TypeSafe API キーを保存
```

ビルド不要。素の JS モジュールで動く。編集したら拡張カードのリロードボタンを押す
（service worker は自動再読込されない）。

## ファイル構成

```
manifest.json    MV3 マニフェスト
extract.js       ページから判定材料を抜き出す（executeScript で注入される関数）
questions.js     質問定義・ラベル辞書・プリセット・閾値。調整はここに集約
background.js    API 呼び出し、キャッシュ、バッジ
popup.html/js    判定結果の表示
options.html/js  APIキー・分類設定・表示設定
```

## 変えないでほしい設計判断

以下は検討の結果そうしてある。変更する場合は理由を確認すること。

### 1. ジャンルは既定6分類。増やすなら軸を増やす

Choice は選択肢上の確率分布を返すため、**意味が重なるラベルが2つあると確率がそこで
割れ、モデルが正しく理解していても confidence が閾値を割る**。「宣伝」「アフィリエイト」
「通販」を並べると 0.4 / 0.35 / 0.15 に散って「判定できず」になる。

解像度が欲しいときはラベルではなく軸を足す。質問は並列評価されるので応答時間は
ほとんど伸びない。現在3軸（`genre` / `stance` / `publisher`）で実質150通り。
「アフィリエイト記事」は `genre=commerce × stance=incentivized × publisher=individual`
として出るので、専用ラベルは要らない。

`GENRE_CATALOG` の `splits` フィールドは親子関係の宣言で、両方有効になると
設定画面が警告を出す。新しいラベルを足すときは `splits` も正しく設定すること。

### 2. instructions と criteria は英語、state は日本語のまま

公式ドキュメントに「主な学習言語は英語で、他の言語では精度が下がる」と明記がある。
質問側だけ英語にしてある。日本語に戻さないこと。

### 3. 数えれば分かるものは Jev に聞かない

アフィリエイトリンク本数、価格表記、CTA 文言、schema.org の型は `extract.js` で
数えて state に入れている。無関係な情報が増えるほど精度が落ちると公式が明記して
いるので、state に何か足すときは「その情報が判断に必要か」を確認すること。

### 4. 既定は手動起動（activeTab）

`content_scripts` で `<all_urls>` に常駐させれば自動判定できるが、その瞬間に
閲覧した全ページの本文が外部 API に流れる。既定は意図的にアイコンを押したページ
だけにしてある。自動化する場合は `optional_host_permissions` で明示的に許可を取る形にする。

### 5. キャッシュキーにラベル構成のハッシュを混ぜてある

`configFingerprint()` の値がキーに入っている。これがないと、分類設定を変えたのに
古い判定が出続けて原因不明のバグになる。

### 6. confidence が低いときは断定しない

`CONFIDENCE.assert` (0.75) 以上で断定、`hint` (0.5) 以上で「〜らしい」、
それ未満は「判定できず」でユーザーに判断を返す。勝手に上位1件を表示する実装に
戻さないこと。

## 未着手（優先順）

1. **日本語ページでの正解率測定。これが最優先。**
   50件程度の正解ラベル付き URL リストを用意し、軸ごとの正解率と confidence 分布を出す。
   閾値はその結果から決める。測る前に公開しない。
2. 階層分類の検討。公式クックブック「classification using confidence」と
   「hierarchical classification」が、確率が割れる問題への公式解。
   詳細プリセットはこれで置き換えられる可能性がある。
   https://docs.typesafe.ai/cookbooks/classification_using_confidence
3. OpenRouter / Vercel AI Gateway 対応。TypeSafe 本体は早期アクセスで
   ウェイトリスト待ちが多いため、エンドポイントと認証を設定で切り替えられるようにする。
   Gateway 経由ならリクエスト単位の Zero Data Retention も指定できる。
4. アイコン 16/48/128 PNG。バッジが乗るので単色の下地でよい。
5. 英語 README。awesome-jev と Discord は英語圏なので英語を本体にする。
6. スクリーンショットか GIF。
7. eval ハーネス（URL リスト → 正解率と confidence 分布）。

## 既知の弱点

- **プロンプトインジェクション。** ページ本文をそのまま state に入れている。
  表示するだけなら実害は限定的だが、判定結果で何かを自動実行する設計にするなら
  Jev の手前に対策が要る。
- **APIキーは平文。** `chrome.storage.local` に置かれるため、拡張を読み込める人は
  取り出せる。各自が自分のキーを入れる公開版では論点にならないが、組織に配布する
  場合は拡張 → 自前プロキシ → Jev の構成にしてキーをサーバ側に置くこと。
- Score は段階の「間」が小数で返る。0/1/2 の整数ではない。

## コードの約束

- ビルドツールを入れない。素の ES モジュールのまま保つ
- 閾値・重み・ラベルは `questions.js` に集約する。他のファイルに定数を散らさない
- Jev に聞く質問は「専門家が数秒で答えられる粒度」に割る
- Choice には必ず「該当なし」を入れる
